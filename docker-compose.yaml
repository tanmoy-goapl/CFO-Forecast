version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: cfo-forecast-ui
    container_name: cfo-forecast-ui-container
    ports:
      - "5173:80"
    restart: unless-stopped